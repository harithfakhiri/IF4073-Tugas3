function  log_img  = log_(img)
    
    figure, imshow(img);
    imgaussfilt(img,1.2);
    kernel = [0 1 0; 1 -4 1; 0 1 0];
    % J = processing_convo(double(I), double(H));
    log_img = uint8(convn(img, kernel));
end